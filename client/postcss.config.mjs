// Tailwind CSS v4 uses a dedicated PostCSS plugin (no tailwind.config.js needed;
// theme is declared in globals.css via @theme).
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
